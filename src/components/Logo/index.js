import PropTypes from 'prop-types';

const LogoSection = () => {
  return (
    <>
      <h4>GYM</h4>
    </>
  );
};

LogoSection.propTypes = {
  sx: PropTypes.object,
  to: PropTypes.string
};

export default LogoSection;
